/* ============================================================
   Aura World — site configuration
   ⚙️  EDIT ONLY THIS FILE to point the site at your accounts.
   Replace GITHUB_KULLANICI_ADIN with your GitHub username, and
   fill in whichever donation methods you actually use (leave the
   rest as "" — empty ones are hidden automatically).
   ============================================================ */
window.AURA = {
  // ⬇️  THE ONLY REQUIRED CHANGE: put your GitHub username here.
  githubUser: "Kaoss7r9er",
  repo: "aura-world",
  version: "1.0.0",

  // Leave empty — the Windows download link is built automatically from the
  // values above as: github.com/<user>/<repo>/releases/latest/download/
  // Aura-World-Setup-<version>.exe  (always points to your newest Release).
  downloadWin: "",

  // Donation methods — fill what you have, leave others empty ("").
  donate: {
    kreosus:  "https://kreosus.com/aura-world/about",
    papara:   "1742600399"        // Papara numarası (Papara→Papara, IBAN gerekmez)
  }
};
